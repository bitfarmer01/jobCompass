// Placeholder — full UI built in Phase 5 (feature 14). Shell + navbar live in
// app/dashboard/layout.tsx.
export default function DashboardPage() {
  return (
    <div className="flex-1 flex items-center justify-center">
      <p className="text-text-secondary text-sm">Profile coming soon.</p>
    </div>
  );
}
